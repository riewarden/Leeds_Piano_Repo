
:: Start
!Welcome to the Leeds Piano Trail!
<<script>> /* This handles the redirection, by putting the search query into varDestination */
 window.grabQuery = function getParameter(parameterName){
    let parameters = new URLSearchParams(window.location.search);
    return parameters.get(parameterName);
    };
//for debugging grabQuery: window.alert(grabQuery("location"));
State.variables.varDestination = grabQuery("location");<</script>>
@@.bigturqButton; [[Tap here for more information.|https://www.leedspiano.com/piano-trail-2021/]]@@
<<if $varDestination is "">>
@@.bigturqButton; [[Tap here to play the piano|Contents]]@@
<<else>>
@@.bigturqButton; [[Tap here to play the piano|$varDestination]]@@
<</if>>
